@{
    ViewBag.Title = "Главная страница";
}

<link href="~/css/home.css" rel="stylesheet" type="text/css" />

<div class="container mt-5 text-center">
    <h1 class="display-4 text-primary">Спорт — это сила!</h1>
    <p class="lead">Занимайтесь спортом, чтобы стать сильнее каждый день.</p>
   
    <div>
        <a href="@Url.Action("Login", "Account")" class="btn btn-primary btn-lg mx-2">Войти</a>
        <a href="@Url.Action("Register", "Account")" class="btn btn-success btn-lg mx-2">Регистрация</a>
    </div>
</div>
